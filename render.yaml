# Configuration du Web Service pour le backend Spring Boot - Runtime natif Render
services:
  - type: web
    name: stock-management-backend
    env: node
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/stock-management-backend-1.0.0.jar --spring.profiles.active=prod
