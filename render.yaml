services:
  # Configuration du Web Service pour le backend Spring Boot
  - type: web
    name: stock-management-backend
    runtime: java
    plan: free
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/stock-management-backend-1.0.0.jar
    envVars:
      - key: JAVA_VERSION
        value: 17
